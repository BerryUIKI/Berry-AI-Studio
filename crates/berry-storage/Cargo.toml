[package]
name = "berry-storage"
version.workspace = true
edition.workspace = true
license.workspace = true
description = "SQLite persistence with schema versioning for Berry AI Studio"

[dependencies]
berry-domain = { path = "../berry-domain" }
rusqlite = { version = "0.40.2", features = ["bundled"] }
serde_json = "1"
thiserror = "2"

[dev-dependencies]
tempfile = "3"

