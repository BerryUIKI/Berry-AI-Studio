# AIGC 元数据与提示词解析

Berry AI Studio 内置了由 Rust 编写的高性能多引擎无损元数据解析核心（`berry-metadata`）。它能够毫秒级自动从主流生成式 AI 平台的输出作品中提取提示词、负向提示词、模型哈希、随机种子以及完整的工作流执行节点图。

---

## 1. 支持的生成式 AI 平台矩阵

Berry 原生支持从 PNG 文本块、WebP EXIF 头以及 MP4 ISOBMFF box 容器中提取以下工具的生成元数据：

| 平台 / 工具引擎 | 自动提取的元数据字段 | 所在媒体容器位置 |
| :--- | :--- | :--- |
| **AUTOMATIC1111 / SD.Next / Forge** | 正向提示词、负向提示词、步数 Steps、采样器 Sampler、CFG 引导系数、Seed 种子、画面尺寸、模型哈希、模型名称、重绘幅度 Denoising、高分放大 Hires Upscale | PNG `parameters` 数据块 / JPEG EXIF `UserComment` |
| **ComfyUI** | 完整节点图、正/负向 CLIP 文本编码、KSampler 步数与种子、Checkpoint 加载器、LoRA 节点、潜空间放大参数 | PNG `prompt` 与 `workflow` 数据块 / WebP 原生数据块 / MP4 `moov/udta` |
| **NovelAI** | 标题、描述、Prompt、Negative Prompt、Seed 种子、采样器、步数、Scale 系数、软件版本号 | PNG `Comment` 与 `Description` 数据块 |
| **Fooocus / Fooocus-MRE** | 基础模型、精炼器 Refiner、LoRA 权重配置、锐化度 Sharpness、性能模式、分辨率、提示词 | PNG `parameters` 文本段 |
| **InvokeAI** | 模型名、VAE、调度器 Scheduler、生成模式 (txt2img/img2img)、无缝贴图平铺设置 | PNG `sd-metadata` 与 `invokeai_metadata` JSON |
| **EasyDiffusion / Stable Swarm** | 格式化 JSON 参数块、Seed 种子、底模名称 | PNG `sui_image_params` / 伴随文件 |

---

## 2. 右侧属性检查器面板 (`InspectorPane.vue`)

当在画廊中选中单张图片或视频时，按下快捷键 `I` 即可展开右侧属性检查器，清晰查看其完整的结构化生成参数：

```
┌────────────────────────────────────────────────────────┐
│ 属性检查器 (参数详情)                              [✕] │
├────────────────────────────────────────────────────────┤
│ [ 媒体高清缩略图预览 ]                                 │
│ 1024 × 1024 · PNG · 3.4 MB · 2026-09-21                │
│ [ ★★★★★ ]  [ ★ 加入收藏 ]  [ 🔞 敏感内容 ]            │
├────────────────────────────────────────────────────────┤
│ 正向提示词 (Prompt)                        [📋 复制]   │
│ ┌────────────────────────────────────────────────────┐ │
│ │ [masterpiece] [1girl] [solo] [cyberpunk city]      │ │
│ │ [neon reflections] [rain] [volumetric lighting]    │ │
│ └────────────────────────────────────────────────────┘ │
├────────────────────────────────────────────────────────┤
│ 负向提示词 (Negative Prompt)               [📋 复制]   │
│ ┌────────────────────────────────────────────────────┐ │
│ │ worst quality, low quality, bad anatomy, bad hands │ │
│ └────────────────────────────────────────────────────┘ │
├────────────────────────────────────────────────────────┤
│ 核心生成技术参数                                       │
│ 模型名称:   animagine_xl_3.1.safetensors               │
│ 模型哈希:   31e35c80  [🔍 在 Civitai 反查]             │
│ 采样器:     DPM++ 2M Karras                            │
│ 采样步数:   28             引导系数 (CFG): 7.0         │
│ 随机种子:   2849104812     [📋 复制]                   │
├────────────────────────────────────────────────────────┤
│ 识别到的 LoRA 列表 (2)                                 │
│ • CyberpunkStyle (权重: 0.85)             [+ 插入提示词]│
│ • DetailedEyes (权重: 0.6)                [+ 插入提示词]│
├────────────────────────────────────────────────────────┤
│ ▼ 原始元数据 (ComfyUI 流程图 JSON)                     │
└────────────────────────────────────────────────────────┘
```

---

## 3. 词元化交互式提示词标签 (Token Chips)

Berry AI Studio 将原本密密麻麻、难以阅读的长篇提示词字符串，自动分词拆解为直观易选的独立芯片标签（Token Chips）：

- **一键反查 (1-Click Search)**：点击任意提示词芯片（例如 `[cyberpunk city]`），画廊会立即执行全库检索，瞬间筛选出包含该主题概念的所有历史生图。
- **一键复制 (1-Click Copy)**：点击提示词卡片右上角的复制图标，即可将干净完整的提示词文本完整拷贝至系统剪贴板。
- **灵感复用**：词元化拆解让你能一眼看穿优质画面的构图用词、光影风格词以及质量修饰词，极其便于在后续创作中复用。

---

## 4. Checkpoint 模型库与哈希反查

AI 生图工具通常在元数据中只内嵌短哈希（如 `31e35c80`）或完整的 SHA-256 字符串，缺乏人类可读的文件名。

- Berry 会自动查询本地的**模型哈希缓存**（SQLite `model_cache` 表），自动将生硬的哈希映射为友好的底模名称（如 `"Animagine XL 3.1"`）。
- 当遇到陌生的未知哈希时，你可以直接一键导入 AUTOMATIC1111 的 `cache.json`，或直接通过 Civitai 进行线上反查匹配（详见[模型库与 LoRA 触发词管理](../04-intelligent-curation/models-and-loras.md)）。

---

## 5. 原始元数据与 ComfyUI 工作流 JSON

针对需要审查节点拓扑结构的资深创作者与技术美术（Tech Artist）：
- 展开检查器最下方的**原始元数据**折叠层，即可审查未经任何修饰的底层原生 JSON 工作流。
- 支持一键复制整个 JSON 结构，以便直接粘贴至文本编辑器或分享给团队协同成员。
